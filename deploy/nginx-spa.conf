# Фрагмент для Nginx: SPA (React Router).
# Подключите в server { } для сайта, например: include /path/to/nginx-spa.conf;
# Либо вставьте location / { ... } в свой server блок.

location / {
    root /var/www/html;
    try_files $uri $uri/ /index.html;
}
